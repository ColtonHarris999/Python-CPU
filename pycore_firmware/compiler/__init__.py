"""On-device ``compile()`` package (PyCore subset).

See ``planning/compiler_design.md``. Ports of PyCPython live here with
PSF-2.0 provenance headers. ``compat.py`` is the rewrite helpers the
subset gate requires; lexer/parser/codegen land in later steps.
"""
